<%= form_for(@school) do |f| %>
  <% if @school.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@school.errors.count, "error") %> prohibited this school from being saved:</h2>

      <ul>
      <% @school.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title, :class => 'my_label' %>
    <%= f.text_field :title, :class => 'my_field' %>
  </div>
  <div class="field">
    <%= f.label :description, :class => 'my_label' %>
    <%= f.text_area :description, :class => 'my_text_area' %>
  </div>
  <div class="field">
    <%= f.label :logo, :class => 'my_label'  %>
    <%= f.file_field :logo %>
  </div>
  <div class="actions">
    <%= f.submit :class => 'btm btn-inverse my_button'%>
  </div>
<% end %>
