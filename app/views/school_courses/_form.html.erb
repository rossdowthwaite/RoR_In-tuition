<%= form_for(@school_course) do |f| %>
  <% if @school_course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@school_course.errors.count, "error") %> prohibited this school_course from being saved:</h2>

      <ul>
      <% @school_course.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :course %><br>
    <%= f.text_field :course %>
  </div>
  <div class="field">
    <%= f.label :school %><br>
    <%= f.text_field :school %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
