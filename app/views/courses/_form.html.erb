<%= form_for(@course) do |f| %>
  <% if @course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course.errors.count, "error") %> prohibited this course from being saved:</h2>

      <ul>
      <% @course.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title, :class => 'my_label' %>
    <%= f.text_field :title, :maxlength => 50,  :class => 'my_field' %>
  </div>
  <div class="field">
    <%= f.label :description, :class => 'my_label' %>
    <%= f.text_area :description, :maxlength => 500,  :class => 'my_text_area' %>
  </div>
  <div class="field">
    <%= f.label :logo, :class => 'my_label' %>
    <%= f.file_field :logo %>
  </div>
  <div class="field">
  <div class="actions">
    <%= f.submit :class => 'btn btn-primary my_button' %>
  </div>
<% end %>
